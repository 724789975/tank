apiVersion: v1
kind: Service
metadata:
  labels:
    app: item-manager
  name: item-manager
  namespace: tank
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    - port: 11001
      targetPort: 11001
      protocol: TCP
      name: rpc
  selector:
    app: item-manager

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: item-manager
  namespace: tank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: item-manager
  template:
    metadata:
      labels:
        app: item-manager
    spec:
      containers:
        - name: item-manager
          image: docker.io/library/item-manager:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
            - containerPort: 11001
              name: rpc
              protocol: TCP
          env:
            - name: DEPLOYENV
              value: test
            - name: CONF_ENV_PATH
              value: /app/etc
            - name: CONF_ENV_FILE
              value: server-test.yaml