apiVersion: v1
kind: Service
metadata:
  labels:
    app: game-server
  name: game-server
  namespace: tank
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    - port: 11001
      targetPort: 11001
      protocol: TCP
      name: rpc
    - port: 10085
      targetPort: 10085
      protocol: TCP
      name: game
  selector:
    app: game-server

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-server
  namespace: tank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-server
  template:
    metadata:
      labels:
        app: game-server
    spec:
      containers:
        - name: game-server
          image: docker.io/library/game-server:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
            - containerPort: 11001
              name: rpc
              protocol: TCP
            - containerPort: 10085
              name: game
              protocol: TCP
          env:
            - name: DEPLOYENV
              value: test
            - name: CONF_ENV_PATH
              value: /app/etc
            - name: CONF_ENV_FILE
              value: server-test.yaml