# HTTP服务配置
server_mgr_http:
  addr: ":8080"

# RPC服务配置
server_mgr_rpc:
  addr: "0.0.0.0:11001"
  service_name: "server-mgr"

# Redis配置
redis:
  addrs:
    - "redis.tank.svc.cluster.local:6389"
  password: "123#@!QAzx"

# etcd配置
etcd:
  addrs:
    - "etcd.tank.svc.cluster.local:2379"
  username: "root"
  password: "123#@!QAzx"

# Tracer配置
tracer:
  address: "otel-collector.open-telemetry-collector:4317"

# 特定服务配置
gateway:
  service_name: "gate-server"

exe_path:
  windows: ".\\etc\\windows.bat"
  linux: "./etc/linux.sh"
  ai_windows: ".\\etc\\ai_windows.bat"
  ai_linux: "./etc/ai_linux.sh"

pod:
  server_addr: "quchifan.wang"
  game_server:
    name: "game-server"
    namespace: "tank"
    image: "docker.io/library/game-server:v1.0.0"
  ai_client:
    name: "ai-client"
    namespace: "tank"
    image: "docker.io/library/ai-client:v1.0.0"